# Workflows Directory

This folder tracks the complex choreographies between individual `Skills` and executing `Tools`. 
It outlines the exact step-by-step logic detailing how, for example, Obsidian Sync patterns combine native Git resolution schemas with Language Model suggestions.
